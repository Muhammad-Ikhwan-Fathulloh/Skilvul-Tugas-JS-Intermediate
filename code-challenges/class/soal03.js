class Network {
    constructor(data, users) {
        this.data = data;
        this.users = users;
    }

    movieTime() {
        if () {

        }
    }

    videoCall() {

    }

    connection() {
        console.log("You must reconnect the Internet Connection")
    }
}

const library = new Network(50, 8)

library.movieTime(5) // returns false
library.videoCall(5) // returns true
library.connection(2); // Run the interval